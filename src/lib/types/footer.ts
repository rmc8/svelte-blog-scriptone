export type Category = {
	id: string;
	name: string;
	count: number;
};
export type Tag = {
	id: string;
	name: string;
	count: number;
};
export type PostCount = {
	yearMonth: string;
	count: number;
};
